@{
    var cartCount = ViewBag.CartCount ?? 3;
    var wishlistCount = ViewBag.WishlistCount ?? 3;
}

<header class="site-header">
    <div class="container header-container">
        <a  asp-controller="Home" asp-action="Index" class="logo">
            <span class="logo-dot">
                <img src="~/logo2.png" alt="Logo" />
            </span>
            <span class="logo-text">Shop<span>Egypt</span></span>
        </a>

        <nav class="main-nav" id="mainNav">
            <a asp-controller="Products" asp-action="Index">Shop</a>
            <a asp-controller="Categories" asp-action="Index">Categories</a>
            <a asp-controller="Home" asp-action="About">About</a>
        </nav>

        <div class="header-icons">
            <a href="#" class="icon-btn" aria-label="Search">
                <i class="fa-solid fa-magnifying-glass"></i>
            </a>

            <a asp-controller="WishListItems" asp-action="Index" class="icon-btn badge-wrap" aria-label="Wishlist">
                <i class="fa-regular fa-heart"></i>
                @if (wishlistCount > 0)
                {
                    <span class="badge mustard">@wishlistCount</span>
                }
            </a>

            <a asp-controller="Account" asp-action="Index" class="icon-btn" aria-label="Account">
                <i class="fa-regular fa-user"></i>
            </a>

            <a asp-controller="Cart" asp-action="Index" class="icon-btn badge-wrap" aria-label="Cart">
                <i class="fa-solid fa-bag-shopping"></i>
                @if (cartCount > 0)
                {
                    <span class="badge olive">@cartCount</span>
                }
            </a>

            <button class="icon-btn mobile-menu-btn" id="mobileMenuBtn" type="button" aria-label="Menu">
                <i class="fa-solid fa-bars"></i>
            </button>
        </div>
    </div>

    <div class="mobile-nav" id="mobileNav">
        <a asp-controller="Shop" asp-action="Index">Shop</a>
        <a asp-controller="Categories" asp-action="Index">Categories</a>
        <a asp-controller="Home" asp-action="About">About</a>
    </div>
</header>